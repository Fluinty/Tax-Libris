# AUDYT REPO — stan zastany (2026-08-12)

Audyt read-only całego `src/`, `supabase/`, konfiguracji i `docs/`, wsparty
introspekcją produkcyjnej bazy przez Supabase MCP. Zero zmian w kodzie —
commit wyłącznie tego raportu. Metoda: 6 niezależnych soczewek krytyka +
adwersaryjna weryfikacja znalezisk KRYTYCZNE/WYSOKIE (część wag skorygowano
po weryfikacji — oznaczone „weryf.").

Format: **waga** | plik:linia | problem | fix | nakład (S/M/L). „(znane)" =
udokumentowane w DECISIONS/task.md/wcześniejszym audycie.

Liczby z bazy potwierdzone 12.08: external_booked **390**, rolled_back **36**
(22 bez resolved_at), klasyfikacja_korekty **2531**, client_kontrahenci **840**,
rules **50**, user_profiles **8**, vat_whitelist_cache **1057**, ai_review_log
**0**, audit_log **19 037** (w tym 13 277 `action='error'`), faktura_events
`auto_candidate` **0**.

---

## 1. Bezpieczeństwo

Wzorce poprawne (do naśladowania): `assertCanWriteClient`, `gateFakturaHistory`,
`fetchClientLearning`, `updateClientData` (whitelist), strony przez
`getAllowedNips` + `applyNipFilter`. Cały dostęp idzie przez
`createSupabaseAdmin` (service_role omija RLS), więc jedyną bramką jest ręczny
kod w akcji — braki są punktowe, ale każdy = natychmiastowy wyciek.

| waga | plik:linia | problem | fix | nakład |
|---|---|---|---|---|
| **KRYTYCZNE** (weryf. REAL) | src/app/actions/zapis-history.ts:6 | `getZapisHistory(zapisId)` bez ŻADNEJ kontroli — każdy zalogowany (w tym rola `klient`) po dowolnym `zapisId` czyta cały audit_log (opisy, kwoty, e-maile księgowych), nazwę dostawcy i `client_nip` dowolnego klienta biura; cross-tenant IDOR tej samej klasy co domknięty 11.08. | Bramka read-only wg `gateFakturaHistory`: blokada roli `klient`, rozwiązanie `client_nip` po `zapisId`, scoping po allowed NIP-ach fail-closed, `.eq('client_nip', …)` na zapytaniach. | M |
| **WYSOKIE** (weryf. REAL) | src/app/(auth)/do-akceptacji/pozycje-actions.ts:99 | `getSimilarPozycje` bez autoryzacji — po cudzym `pozycjaId` przez RPC `match_pozycje` zwraca historyczne pozycje (nazwy, kwoty, klasyfikacje) dowolnego klienta; drugi IDOR, w kontraście do `updatePozycjaWymiar` z `assertCanWritePozycja`. | Read-only odpowiednik `assertCanWritePozycja` (rozwiązać `client_nip` pozycji + scoping) przed RPC. | S |
| **WYSOKIE→ŚREDNIE** (weryf. REAL, waga↓) | src/app/(auth)/wyjatki/actions.ts:45 | `resolveException`/`ignoreException`/`addProponowanyToClientOpisy` autoryzują inny obiekt (`assertCanWrite` rozwiązuje `faktury.id`) niż zapisują (`exceptions_queue` po `.eq('id', exceptionId)`) — sekwencje id się nakładają → dostęp do własnej faktury id=X pozwala tknąć cudzą kartę queue id=X. Martwe w UI, ale `'use server'` = żywe endpointy POST. | Usunąć martwe akcje (zostawić `toggleAutoWrite`) — patrz §3. | M |
| ŚREDNIE | src/app/(auth)/do-akceptacji/actions.ts:977 | `checkExceptionStatus` bez kontroli roli/NIP — enumeracja statusów kart wszystkich klientów po id. | Lekka bramka read-only (getAllowedNips + scoping). | S |
| ŚREDNIE | src/app/(auth)/do-akceptacji/actions.ts:869 | `updateJpkSection`/`resetJpkSection`/`updateFinalZapisVAT` bez guardu statusu — można nadpisać `final_*` (w tym `final_kwoty_per_kolumna`) karty już zaksięgowanej; wartości niewalidowane (dowolny JSON → baza → worker). | Dodać `.in('status', ['pending','pending_review'])` do wszystkich update'ów + walidacja wartości. | M |
| ŚREDNIE | src/app/(auth)/do-akceptacji/actions.ts:358 | `approveWithEdit`/`approveExceptionFull`/`resolveException` przyjmują `kwoty` bez whitelisty kluczy i walidacji — dowolne klucze kolumn i `NaN` (po `Number()`) trafiają do `final_kwoty_per_kolumna`, którą worker księguje; opis bez limitu długości. | Whitelist kolumn KPiR (jak `KPIR_LABELS`), odrzucanie nie-finite, limit opisu. | M |
| ŚREDNIE | src/app/(auth)/klienci/[nip]/actions.ts:233 | `updateAutoWriteSettings` (bramka auto-write!) nie waliduje `auto_max_kwota` — `NaN`/`Infinity`/ujemna trafia do `clients.auto_max_kwota`, którą worker porównuje w `auto_write_gate`; `nip` bez walidacji formatu. | `Number.isFinite(x) && x > 0` (+ górny limit) i `/^\d{10}$/` przed UPDATE. | S |
| ŚREDNIE | src/app/login/actions.ts:9 | `checkWhitelist` to nieuwierzytelniona wyrocznia: rozróżnia „brak dostępu"/„dezaktywowane"/allowed → enumeracja e-maili z `panel_users`. | Jedna generyczna odpowiedź niezależnie od istnienia konta + rate-limit per IP. | S |
| ŚREDNIE | src/app/(auth)/do-akceptacji/actions.ts:1092 | `fetchFakturaEvents`/`fetchFakturaTimeline` interpolują `fakturaId/queueId` do `.or('faktura_id.eq.${…}')` bez walidacji int — string z przecinkiem = wstrzyknięcie filtra PostgREST (dla admina bez `.eq(client_nip)` idzie z wstrzykniętym filtrem). | `Number.isSafeInteger(x) && x > 0` na wejściu obu akcji i gate'u. | S |
| ŚREDNIE (decyzja) | src/app/(auth)/logs/page.tsx:19 | Żadna strona nie blokuje ODCZYTU dla roli `klient` (DECISIONS 2026-08-11 mówi tylko o zapisie) — klient z przypisanym NIP-em widziałby audit_log/client_changes_log (przebieg pracy biura, e-maile), wyprzedzając „portal klienta". | Layout `(auth)` czyta `panelUser.rola` i dla `klient` blokuje /logs, /dashboard (server-side), albo jawny wpis w DECISIONS że read-all-w-zakresie-NIP jest zamierzone. | M |
| KOSMETYKA | src/app/(auth)/klienci/[nip]/actions.ts:163 | `addPojazd`/`updatePojazd`/`updateOpis` sanityzują blacklistą (tylko `client_nip`/`id`) — księgowa może patchem ustawić `created_by`, `hit_count`, `data_dodania`. | Whitelist kolumn per tabela (wzór `CLIENT_EDITABLE_FIELDS`). | S |
| KOSMETYKA | src/app/auth/callback/route.ts:8 | `next` z query bez walidacji do `redirect(\`${origin}${next}\`)` — konkatenacja chroni przed `//evil`, ale backslash/przyszły `new URL` to open-redirect po magic-linku. | Akceptować tylko `next` zaczynające się od `/` i nie od `//`/`/\`. | S |
| KOSMETYKA | src/lib/supabase/browser.ts:5 | Podejrzenie „PojazdyTable zapisuje przez klient browser" **NIEPRAWDZIWE** — zapisy idą przez server actions z `assertCanWriteClient`; anon czyta tylko `panel_users` do gatingu UI (i tak failuje, bo `authenticated` nie ma grantów na `fluinty`). Granica szczelna; martwy fallback do usunięcia. | Usunąć martwy `useEffect` czytający `panel_users`, przekazywać `isAdmin` z serwera. | S |
| KOSMETYKA (spójność) | fluinty.faktura_events (RLS) | Jedyna tabela `fluinty` z wyłączonym RLS (dziś nieeksploatowalne — brak grantów dla anon/authenticated), ale niespójność defense-in-depth. | Włączyć RLS (migracja u właściciela), service_role i tak omija. | S |

**Uwaga systemowa:** brak wzorca wymuszającego autoryzację przy każdym nowym
eksporcie akcji — warto wprowadzić obowiązkowy helper/konwencję lint, bo
pominięcie w jednym eksporcie (zapis-history) natychmiast staje się wyciekiem.

---

## 2. Zgodność z DECISIONS.md / CLAUDE.md

| waga | plik:linia | problem | fix | nakład |
|---|---|---|---|---|
| **KRYTYCZNE→WYSOKIE** (weryf. REAL, waga↓) | src/app/(auth)/do-akceptacji/actions.ts:269 | Czysty klik Zatwierdź kopiuje ai→final: `kwotyDoZapisu` fallbackuje do `ai_kwoty_per_kolumna`, `mergeInlineEditsVat` bez edycji zwraca kopię `ai/zapis_vat_data` → `final_*` zapisane wartościami AI w OBU tabelach, wprost wbrew DECISIONS 2026-08-05 („final zostaje null"). Weryf.: metryka `pct_ksiegowa` liczona z flag (poprawnych), więc auto-write NIE skażony; szkoda = złamany kontrakt + mylący badge „kandydat auto" na /klienci/[nip] (myli się w bezpieczną stronę). | Zapisywać `final_*` tylko przy realnej edycji (inaczej jawnie null) — po potwierdzeniu na K1, że worker przy `final=null` księguje z `ai_*`. | M |
| **KRYTYCZNE** (weryf. REAL) | src/app/(auth)/do-akceptacji/actions.ts:222 | `resolveExceptionIds` połyka błędy odczytu `faktury` (l.203/211 bez `error`), a przy `faktura=null` robi `queueId = exceptionId` → przejściowy błąd odczytu zamienia `faktury.id` w `queue.id` i kolejny UPDATE `exceptions_queue.eq('id', queueId)` może zatwierdzić CUDZĄ kartę pending (łamie regułę legacy_id + zakaz połykania błędów). | Sprawdzać `error` obu odczytów; fallback „czysty queue-id" tylko przy `data===null && error===null`. | S |
| **WYSOKIE** (weryf. REAL) | src/app/(auth)/do-akceptacji/actions.ts:905 | `updateJpkSection` połyka błąd zapisu gdy istnieje drugi klucz: `if (error && !queueId) return` → dla normalnej karty nieudany UPDATE `exceptions_queue` (tabeli, z której worker księguje!) raportowany jako success; to samo `resetJpkSection` (947/956), `updateFinalZapisVAT` (606). Księgowa myśli, że zmieniła GTU/VAT/reżim — nie zmieniła. | Zwracać błąd (z nazwą tabeli), gdy KTÓRYKOLWIEK z dwóch zapisów padnie. | S |
| **WYSOKIE→ŚREDNIE** (weryf. REAL, waga↓) | src/app/(auth)/do-akceptacji/actions.ts:316 | Sync statusu/finali do `faktury` w approveFaktura (316-330) i analogicznie w approveWithEdit/approveExceptionFull/resolveException/ignoreFaktura bez odczytu `error` — cicha desynchronizacja `faktury` vs `exceptions_queue`. Weryf.: worker księguje z `exceptions_queue` (error-checked), więc to zapis wtórny → rozjazd UI/historii, nie korupcja księgowania. | Destrukturyzować `{ error }` i dokładać warning (wzorzec `logFakturaEvent`). | S |
| ŚREDNIE | src/app/(auth)/do-akceptacji/page.tsx:75 | DEBUG na produkcji: `console.log('[do-akceptacji] rawExceptions…')` (75) i (135); w dodatku `rawError` głównej listy trafia WYŁĄCZNIE do loga → przy awarii kolejka renderuje się pusto bez komunikatu. | Usunąć oba `console.log`, `rawError` obsłużyć w UI. | S |
| ŚREDNIE | src/app/(auth)/dashboard/page.tsx:54 | Systematyczne połykanie `error` w odczytach: dashboard 54/101/124/150/161, do-akceptacji 79/92/147/166/180/203/221, klienci/[nip] 36/63/67/105/108/115/135, faktury 41/46, klienci 35, logs 49, actions/search 24-72, zapis-history 13-47 — awaria bazy = puste UI zamiast komunikatu (konwencja CLAUDE.md). | Wszędzie `{ data, error }` + komunikat (wspólny `throwIfError(tabela)`). | M |
| ŚREDNIE | src/app/(auth)/do-akceptacji/actions.ts:24 | `logAudit` insertuje bez sprawdzenia `error` (ślad audytowy znika po cichu); to samo w wyjatki 57/109/269 i pozycje-actions 61 — niespójne z decyzją 2026-08-11 o niepołykaniu błędów osi czasu. | Wzorzec `logFakturaEvent`: `error` → console.error + warning w zwrotce. | S |
| ŚREDNIE | src/components/…/FakturaHistoriaSection.tsx:96 | Status `rolled_back` (36 kart w prod) spoza listy CLAUDE.md (docs: `rollback_requested`/`rollback_failed`); mapowania UI obsługują tylko `rollback`/`rollback_requested` — słownik statusów repo↔worker rozjechany. | Uzgodnić słownik z workerem (K1) i dopisać do docs/typów/UI. | S |
| KOSMETYKA | src/app/(auth)/do-akceptacji/actions.ts:981 | `checkExceptionStatus` połyka błędy obu odczytów → pre-check anty-double-approve po cichu przestaje chronić przy awarii. | Zwracać rozróżnialny błąd. | S |
| KOSMETYKA | src/lib/auth-helpers.ts:136 | `assertCanWrite` połyka `error` trzech odczytów (fail-closed, ale komunikat kłamie przy awarii bazy). | Rozróżnić error od braku wiersza. | S |
| KOSMETYKA | src/app/api/cron/healthcheck/route.ts:64 | Watchdog połyka błędy antyspamu (odczyt/upsert `config.watchdog_last_alert`) → spam alertów lub cichy brak cooldownu. | Sprawdzić error + console.error. | S |

---

## 3. Spójność i martwe ciało

**FakturaPreview (kanoniczny) vs PelnaFakturaSection — łamanie „zmiany idą do OBU":**

| waga | plik:linia | problem | fix | nakład |
|---|---|---|---|---|
| **WYSOKIE→ŚREDNIE** (weryf. REAL, latentne) | FakturaPreview.tsx:22 | Kanoniczny podgląd dokleja na sztywno „zł" (`fmtKwota`), a PelnaFaktura renderuje `kodWaluty` — faktura EUR/USD pokaże księgowej „zł". Dziś 0 faktur nie-PLN na 1627 (latentne). | Odczytać `kodWaluty` z `podglad_faktury`, przekazać do `fmtKwota` (fallback „zł"). | S |
| **WYSOKIE→ŚREDNIE** (weryf. REAL, latentne) | PelnaFakturaSection.tsx:122 | Badge Korekta/Zaliczka/Marża/MPP i alert rabatu nagłówkowego istnieją TYLKO w zwijanej PelnaFakturze — księgowa może zatwierdzić korektę/MPP nie widząc tego bez rozwinięcia. Dziś 0 korekt/zaliczek/marż i 4 MPP <15k. Ryzyko prospektywne (korekty KSeF będą). | Dodać te same badge do FakturaPreview albo jawny komentarz „świadomy wyjątek" w obu. | M |
| ŚREDNIE | FakturaPreview.tsx:199 | Zestawienie VAT w Preview to inline-duplikat `getOfficialVatTable`, ale BEZ `normalizeStawka` — ta sama faktura może dać inne grupowanie stawek niż PozycjeVatSection/PelnaFaktura. | Wywołać `getOfficialVatTable` + `normalizeStawka` z `@/lib/vat`. | M |
| ŚREDNIE | FakturaPreview.tsx:256 | Wiersz różnicy (Layer 2) liczony z różnych baz: Preview `kwota_brutto` − suma XML, PelnaFaktura `kwotaDoZaplaty` − officialVatTable → przy rozjeździe jeden pokaże saldo, drugi nie. | Policzyć `layer2Diff` raz (helper w vat.ts) i renderować w obu. | M |
| ŚREDNIE | PelnaFakturaSection.tsx:140 | Dalsze rozbieżności bez komentarza „świadomy wyjątek": rabaty per pozycja, `dodatkowyOpis` z badge, kwota VAT w pozycjach, wiersz „Ogółem" tylko w Pełnej; numer KSeF, strony, termin+forma płatności, kolumny Cena/J.m. tylko w Preview; sekcja Rozliczenia w innym formacie. | Doimplementować w drugim albo opatrzyć komentarzem. | M |

**Duplikacja logiki i martwy kod:**

| waga | plik:linia | problem | fix | nakład |
|---|---|---|---|---|
| **WYSOKIE** | src/lib/audit-formatter.ts:36 | Dwa pliki-bliźniaki: `audit-format.ts` (310, RecentActivityList) i `audit-formatter.ts` (180, ZapisHistorySheet) trzymają RÓWNOLEGŁE switch'e po tym samym zbiorze akcji audit_log — nowa akcja w jednym nie dostanie etykiety w drugim. | Scalić do jednej mapy akcja→label/kolor/ikona. | M |
| **WYSOKIE** | src/components/…/FakturaCard.tsx:655 | Blok „effective pozycje VAT" (655-697) to niemal dosłowny duplikat `mergeInlineEditsVat` (67-102) — zmiana reguł wykluczeń w jednym miejscu → księgowa widzi inne kwoty niż panel zapisze do `final_zapis_vat_data`. | Wspólny `computeEffectivePozycjeVat` w merge-helpers, użyć w obu. | M |
| ŚREDNIE (znane) | src/app/(auth)/wyjatki/actions.ts:8 | Martwe akcje bez importera: `resolveException` (8), `ignoreException` (75), `addProponowanyToClientOpisy` (210) + `approveWithEdit` (do-akceptacji:358). Każda robi UPDATE `exceptions_queue` → realne ryzyko przy refaktorze. | Usunąć 4 martwe akcje (`toggleAutoWrite` przenieść). | S |
| ŚREDNIE | src/app/(auth)/do-akceptacji/pozycje-actions.ts:82 | NOWE martwe (poza znanymi): `updatePozycjaKpir` (82) i `checkWhitelist` (login/actions:9) — bez importera, oba z dostępem do bazy. | Usunąć albo podpiąć. | S |
| ŚREDNIE | src/lib/vat.ts:141 | Dwa słowniki tej samej domeny: EditModal używa `PROCEDURY_JPK`/`GRUPY_ASORTYMENTU` z vat.ts, inline sekcje `PROCEDURY_JPK_BITMASK`/`GTU_LIST` z jpk-helpers — różne listy kodów w modalu vs inline. | Ujednolicić na jpk-helpers (bitmask), usunąć z vat.ts. | M |
| ŚREDNIE | src/types/database.ts:160 | Union `status` w `ExceptionItem` nie zawiera `external_booked` (390!), `rolled_back` (36) ani `rollback_*`, choć kod porównuje do `external_booked`; typy-widma bez użycia: `AuditLog`, `PanelUserRola`, `WolumenInvoiceRecord`, `ClientChange`, `ClientFull`. | Uzupełnić union o realne statusy, usunąć 5 martwych interfejsów. | S |
| ŚREDNIE | README.md:1 | README opisuje nieistniejący produkt: „Next.js 16" (jest 15.5), trasa `/reguly` (brak), narracja o „regule i automacie" przy `rules`=50 martwych wierszy i martwym module wyjatki; brak wzmianki o ręcznym `migrations/`. | Przepisać do stanu faktycznego albo skrócić i odesłać do docs/. | S |
| ŚREDNIE | task.md:1 | Sekcja „Implementacja Zapisu VAT" ma puste checkboxy, choć wdrożona; `implementation_plan.md`/`walkthrough.md`/`test-diff.js` to historyczne artefakty. | Wyciąć wykonane, artefakty do docs/archive lub usunąć. | S |
| KOSMETYKA | src/components/exceptions/SortDropdown.tsx | Martwe: `SortDropdown` (bez importera), nieużywane shadcn `pagination/scroll-area/table/tabs`, martwe eksporty `bitmaskToGtuNums`/`gtuNumsToBitmask`, `RODZAJE_ZAKUPU`/`RODZAJE_ODLICZENIA`/`CELE_ZAKUPU` (zastąpione `*_LABELS`). | Usunąć plik + 4 ui/* + martwe eksporty. | S |
| KOSMETYKA (TODO) | PojazdRezimSection.tsx:346 | Jedyne TODO: alert `leasing_bez_danych` czeka na flagę workera; `AiReviewSection`/`ai-review.ts` renderują `ai_review_log` (0 wierszy — reviewer v1 wyłączony). Flow świadomie niedokończony. | Dopisać oba do Backlogu task.md z zależnością „strona workera". | S |

---

## 4. Jakość ścieżek krytycznych

| waga | plik:linia | problem | fix | nakład |
|---|---|---|---|---|
| **KRYTYCZNE** (weryf. własna w kodzie) | FakturaCard.tsx:267 | Efekt keydown ma deps `[isActive, stan, selectedOpis, showEditModal]`, a `handleApprove` czyta `currentKpirKwoty`/`kpirEditedManually`/`editableOpis` (292-301), których w deps NIE MA → stale closure: po inline-edycji kolumny KPiR (100→200) lub opisu i kliknięciu poza input, **Enter księguje wartości sprzed edycji** (ścieżka przyciskiem OK). Ścieżka EditModal ratuje przypadkiem (`showEditModal` w deps). | `handleApprove/Resolve/Ignore` w ref (`handlerRef.current=…`) i wołać z ref, albo dołożyć stany do deps. | S |
| **KRYTYCZNE** | do-akceptacji/actions.ts:288 | Guard statusu (`.in('status',[pending…]).select('id')` + pusty=błąd) jest TYLKO na update `exceptions_queue`. Karta faktury-native (`queueId=null`) omija go w całości, a update `faktury` (315) nie ma warunku statusu → podwójny Enter/stała lista może nadpisać już zaksięgowaną kartę na `approved` (dla rekordów faktury-native worker księguje z `faktury`). Do sprawdzenia udział takich kart. | Dodać `.in('status',[…]).select('id')` do update `faktury` w 4 akcjach i zwracać „Faktura zmieniła status". | M |
| **WYSOKIE** | EditModal.tsx:130 | `handleSave` odrzuca `num <= 0` → faktura korygująca z ujemnymi kwotami AI po „Zatwierdź zmiany" zapisuje `{}` (kwota znika); regex `/^[\d.,]*$/` blokuje minus. | `num !== 0` zamiast `> 0` i dopuścić minus, albo jawnie zablokować modal dla korekt. | S |
| **WYSOKIE** | FakturaCard.tsx:201 | `handleKpirKwotaChange`: `parseFloat("123,45")` → **123** (polski przecinek ucięty) → kolumna KPiR księgowana bez groszy; UI pokazuje 123,45, Enter księguje 123,00. Ten sam błąd `PozycjeVatSection.tsx:182`. | `parseFloat(value.replace(',', '.'))` lub normalizacja w `CalcInput`; wspólne `parsePolishNumber`. | S |
| **WYSOKIE** | calc-expression.ts:30 | Ścieżka „czysta liczba": `parseFloat` na całym stringu bez walidacji reszty — `"1 234,56"`→1, `"12.34.56"`→12.34, cicho złe wyjście (z operatorem spacje usuwane, bez — nie). | Usunąć spacje i walidować `^-?\d+(\.\d+)?$`, przy niedopasowaniu błąd. | S |
| **WYSOKIE** | merge-helpers.ts:71 | Blok `hasExclusions` przelicza `pozycje_vat` z pozycji i nadpisuje wcześniej scalone `pozycje_vat_final` — ręczna korekta tabeli VAT znika przy approve, gdy karta ma ≥1 pozycję nkup/brak; pętla po oryginalnych stawkach → nowa stawka gubiona z sum rejestru. | Przy `pozycje_vat_edited=true` nie nadpisywać ręcznych; nowe stawki dodawać z `stawka_id=null`. | M |
| **WYSOKIE** | types/database.ts:160 | Rozjazd rollback: ARCHITECTURE obiecuje `rollback_requested → pending_review\|rollback_failed`, panel NIGDZIE nie ustawia `rollback_requested`, w prod jest `rolled_back` (36), union typu nie zna `external_booked`/`rolled_back` → /faktury pokazuje te wiersze bez badge i filtra. | Uzgodnić kontrakt (docs+typy+UI); jeśli rollback z panelu planowany — odnotować że dziś robi go worker. | M |
| ŚREDNIE (znane) | do-akceptacji/actions.ts:536 | `approveExceptionFull` nie zapisuje `final_kwoty_per_kolumna` do `faktury` (tylko queue) — rozjazd źródeł (rekomendacja 5 audytu 12.08). Weryf.: skutek to rozjazd wyświetlania (3/40 kart, grosze), utrata edycji wymaga cyklu rollback→re-approve. | Dodać `final_kwoty_per_kolumna` do update `faktury`. | S |
| ŚREDNIE (znane) | do-akceptacji/actions.ts:128 | `buildEditDiff` ignoruje argumenty `finalVat`/`finalPojazd`; `updateFinalZapisVAT` bez flag i eventów → panelowa edycja VAT nie wchodzi do diffu/osi czasu (ratuje przeliczenie flag przez workera przy księgowaniu, niepotwierdzone na K1). | Porównywać `final_zapis_vat_data`/`final_kpir_pojazdowe_data` w `buildEditDiff` albo usunąć martwe parametry. | M |
| ŚREDNIE | do-akceptacji/actions.ts:736 | `ignoreFaktura` bez guardu statusu ani pre-checku — Escape na karcie, którą ktoś zatwierdził/worker zaksięgował, nadpisuje status na `ignored` (Rachmistrz ma zapis, panel mówi „pominięto"). | `.in('status',[pending…]).select('id')` do obu update'ów + błąd przy pustym. | S |
| ŚREDNIE | do-akceptacji/actions.ts:659 | `resolveException` jako jedyna ścieżka ignoruje `isDemo` → karta demo pending dostaje `resolved` zamiast `auto_created` (a statusu `resolved` w prod nie ma → demo-karta może utknąć). | `targetStatus = isDemo ? 'auto_created' : 'resolved'`. | S |
| ŚREDNIE | merge-helpers.ts:37 | `if (exception.pozycje_vat_final)` traktuje `[]` jako edycję → `pozycje_vat=[]`, sumy=0 do `final_zapis_vat_data` (utrata odliczenia VAT). | Warunek `?.length > 0`. | S |
| ŚREDNIE | demo-seed.ts:6 | Brak transakcyjności resetu/seedu demo (~9 DELETE + INSERT-y w pętli) — błąd w połowie zostawia stan częściowy (eventy skasowane, karty zostają). | Opakować w RPC/plpgsql (jedna transakcja) albo kompensacja przy błędzie. | L |
| ŚREDNIE | demo-seed.ts:201 | Seed ustawia `final_*` jako kopie AI na kartach pending → łamie „final tylko z realnej edycji"; przy approve demo `audit_log` raportuje `source='monika_edits'` mimo zero edycji. | Nie wypełniać `final_*` w seedzie dla kart niezakończonych. | S |
| ŚREDNIE | do-akceptacji/actions.ts:278 | `opisDoZapisu = overrideOpis \|\| ai_proponowany_opis`: wyczyszczenie opisu wraca po cichu do AI, a ścieżka bliźniacza (`approveExceptionFull`) zapisuje pusty string — ten sam Enter daje różny `resolved_opis` zależnie od tego, czy kwoty się różnią. | Ujednolicić kontrakt: `undefined`=bez zmiany→AI, `''`=celowe→walidacja. | S |
| ŚREDNIE | kpir.ts:128 | `computeKpirFromPozycje` nie obsługuje `czesciowe_25` (jest w typie i merge-helpers) → liczone jak pełne odliczenie zamiast 20%×(netto+½VAT). | Gałąź `czesciowe_25 → 0.20*(netto+vat*0.5)`. | S |
| ŚREDNIE | merge-helpers.ts:139 | `normalizujRezim` dla stringów z „leasing" bierze PIERWSZĄ liczbę (regex bez kotwicy): „leasing limit 150 tys proporcja 75%"→150→„100"; próg pct≥95 mapuje „96%" na pełne 100. | Szukać liczby przed `%`; przy niejednoznaczności null. | S |
| KOSMETYKA | merge-helpers.ts:163 | `roundKwoty`: `null` w mapie → 0 (`null*100`); `Math.round(v*100)/100` na x.xx5 traci grosz (1.005→1.00), ujemne zaokrągla ku +∞; `NaN` przechodzi do zapisu. Ten sam wzorzec w calc-expression/vat.ts. | `Math.round((v+ε·sign)*100)/100` + `Number.isFinite` guard, jeden wspólny helper. | S |
| KOSMETYKA | EditModal.tsx:432 | Dwa formaty `grupa_asortymentu`: EditModal zapisuje string `'GTU_01'`, GtuSection/merge — numeryczny bitmask → worker dostaje raz string, raz liczbę. | Ujednolicić na bitmask (`gtuNumsToBitmask` przed zapisem). | S |

---

## TOP 5 — do zrobienia najpierw

1. **[KRYTYCZNE] Domknąć IDOR `getZapisHistory`** (`src/app/actions/zapis-history.ts:6`;
   przy okazji `getSimilarPozycje`, `pozycje-actions.ts:99` — ta sama klasa).
   Żywy, wywoływany endpoint bez żadnej bramki przez service_role wyciąga
   audit/kwoty/e-maile dowolnego klienta biura — dokładnie ten typ IDOR, który
   zespół świadomie zamykał 11.08 w historii faktur. Nakład M, wzorzec gotowy
   (`gateFakturaHistory`). Największe ryzyko: realny wyciek między klientami.

2. **[KRYTYCZNE] Naprawić stale-closure skrótów w `FakturaCard`**
   (`FakturaCard.tsx:267`). Enter po inline-edycji kolumny KPiR/opisu księguje
   **kwoty sprzed edycji** do Rachmistrza — cichy błąd poprawności na
   najczęstszej interakcji (Enter reklamowany w legendzie karty). Nakład S
   (handler w ref). Priorytet, bo dotyka pieniędzy i jest niewidoczny dla
   księgowej.

3. **[KRYTYCZNE] `resolveExceptionIds`: przestać połykać błąd odczytu**
   (`actions.ts:222`). Przejściowy błąd odczytu `faktury` zamienia `faktury.id`
   w `queue.id` i kolejny UPDATE może zatwierdzić CUDZĄ kartę pending —
   złamanie reguły legacy_id i zakazu połykania błędów naraz. Nakład S.

4. **[WYSOKIE] Zatrzymać ciche połykanie błędu zapisu do `exceptions_queue`**
   w `updateJpkSection`/`resetJpkSection`/`updateFinalZapisVAT`
   (`actions.ts:905/947/606`). To tabela, z której worker księguje: nieudany
   UPDATE raportowany jako success = księgowa myśli, że poprawiła GTU/VAT/reżim,
   a księguje się stara wartość. Nakład S. (Domknąć razem z guardem statusu z
   §4 poz. 2 — ta sama rodzina akcji.)

5. **[WYSOKIE] Rozstrzygnąć kopiowanie ai→final przy czystym kliku**
   (`actions.ts:269`). Wprost łamie żelazną zasadę („final zostaje null"),
   zatruwa badge „kandydat auto" i jest miną dla każdego przyszłego konsumenta
   ufającego docs. Metryka `pct_ksiegowa` dziś bezpieczna (liczona z flag), więc
   to nie pożar księgowania — ale kontrakt trzeba albo przywrócić (final=null),
   albo formalnie zmienić w DECISIONS po potwierdzeniu na K1, że worker księguje
   przy `final=null`. Nakład M.

**Poza Top 5, ale tanie i warte pilnie:** polski przecinek w `parseFloat`
(`FakturaCard:201`, `PozycjeVatSection:182`, `calc-expression:30`) —
zaniżone o grosze/setki kwoty księgowane, nakład S; usunięcie 4 martwych
akcji z `wyjatki/actions.ts` (domyka też IDOR z §1), nakład S.
